#include <iostream>
using namespace std;

int main(int argc, char const *argv[])
{

    // (a+b+c)2 = a2 + b2 + c2 + 2ab+2ac+2bc
    // = 2(c2 + bc + ac + ab)
    // = 2(c(b+c) + a(b+c))
    // = 2((b+c)(a+c))
    // = 2((1000-a)(1000-b))

    return 0;
}
